//this class is a representation of a cart, made because the localStorage way only save Strings data, this is good to the phone performance because

class CartAux {
  final String productID;
  final String count;

  CartAux({required this.productID,required this.count});
}